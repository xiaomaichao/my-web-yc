# 占彦超个人作品集｜项目进度

更新时间：2026-09-05

## 项目位置与运行

- 项目目录：`C:\Users\zhanyanchao\Documents\ChatGPT\个人主页`
- 技术栈：React 19 + Vite 8 + GSAP / ScrollTrigger
- 本地开发：`pnpm run dev`
- 生产构建：`pnpm run build`
- 当前本地地址：`http://127.0.0.1:5173/`
- 手机目前不能通过 `127.0.0.1` 直接访问；局域网测试需要把 Vite host 改为 `0.0.0.0`，正式外网访问需要部署。

## 当前页面结构

1. 全屏首屏 Hero：背景视频、ZHAN / YAN CHAO 主标题、导航与合作入口
2. 关于我与工作经历
3. 个人优势
4. 精选作品
   - 项目管理（开发流程 / 资源管理 / 进度管理）
   - 游戏 UI 设计
   - 游戏视频
   - 角色与世界观
5. 整屏联系方式收尾页

导航栏滚动到第二屏后固定在顶部，并使用磨砂玻璃样式。

## 已完成的视觉与动效

- 首屏标题经过倾斜排版，`YAN CHAO` 使用透明镂空描边，可透出背景视频。
- 调整了首屏标题字号和安全空间，避免描边与阴影被裁切。
- 导航悬浮时出现黄色文字、微光和短下划线。
- 各模块使用 GSAP 遮罩揭示、位移和卡片错峰进场。
- 手机端关闭持续视差；减少动效模式会停用复杂动画。
- 个人优势卡片使用持续流光边框，离屏后自动暂停。
- 人物图固定为高饱和状态，不再循环改变饱和度。
- 项目入口与人物图使用圆角。
- 在游戏 UI 设计上方新增独立的“项目管理”板块，包含开发流程、资源管理和进度管理三个可切换标签，支持方向键、Home 和 End 键操作。
- “项目管理 → 开发流程”下方的游戏 2D 美术流程内容暂时隐藏，等待后续确定展示内容后再设计。
- 项目管理三个主选项右侧加入放大后的圆角横向图片窗口，使用现有作品作临时内容并持续循环滚动；右下角提供“查看大图”按钮，每张滚动图片也可以单独打开查看。
- 项目管理轮播图全部替换为 `F:\个人资料\项目管理轮播图` 中的 8 张图片：开发流程 3 张、资源管理 2 张、进度管理 3 张；保留 PNG 清晰度并完整显示画面。
- 异世界生物图鉴一次完整展示一张图，进入可视区域后循环切换；DOM 仅保留当前图片并预加载下一张。

## 视频与封面对应关系

- V.01 赤暝黑骑：`knight.mp4` → `poster-knight.jpg`
- V.02 冰霜：`frost.mp4` → `poster-frost.jpg`
- V.03 樱花骷髅武士：`sakura-slash.mp4` → `poster-sakura-slash.jpg`，横屏大卡片
- V.04 美漫卡通新娘：`cartoon-bride.mp4` → `poster-cartoon-bride.jpg`
- V.05 朽樱丸：`sakura-gift.mp4` → `poster-sakura-gift.jpg`

五张封面均从对应视频中截取并压缩，单张约 120–213 KB。

## 性能优化记录

- 图片素材总量约 11.93 MB，均为 WebP；懒加载并使用异步解码。
- 视频素材总量约 59.06 MB，其中首屏 `city.mp4` 约 20.74 MB。
- 首屏视频仅在浏览器空闲且接近首屏时挂载；手机、2G、节省流量和减少动效模式使用静态封面。
- 五个作品视频使用 `preload="none"`，点击播放前不读取视频数据。
- 移除页面自身持续执行的 `scroll` / `resize` 监听，改用 `IntersectionObserver`。
- 作品弹窗按需加载，未点击项目前不进入首屏 JavaScript。
- 移除未使用的 Tailwind 全量样式。
- CSS：约 179.33 KB 降至 36.04 KB，Gzip 约 8.24 KB。
- 首屏 JavaScript：约 429.04 KB 降至 342.22 KB，Gzip 约 118.29 KB。
- 当前生产构建通过，浏览器控制台无警告或错误。

## 关键文件

- `src/App.jsx`：页面结构、导航、视频加载与轮播逻辑
- `src/content.js`：经历、项目、视频和能力数据
- `src/styles.css`：页面全部视觉和响应式样式
- `src/usePortfolioMotion.js`：GSAP 与 ScrollTrigger 动效
- `src/ProjectDialog.jsx`：按需加载的作品详情弹窗
- `components/BorderGlow.jsx` / `components/BorderGlow.css`：个人优势流光卡片
- `public/assets/`：图片、视频与视频封面

## 下次继续建议

- 根据新的参考截图继续调整排版、间距和动效节奏。
- 如需手机真机预览，开放 Vite 局域网 host，并检查 Windows 防火墙与电脑局域网 IP。
- 如需公开访问，配置正式部署和缓存策略。
- 如果安装视频编码器，可进一步压缩 `city.mp4` 和五个作品视频的物理体积。

- 章节标签中的中文标题（关于我、个人优势、精选作品、联系我）已单独放大，英文元信息保持原尺寸。

- 章节中文标题进一步强化：18px 半粗白字、黄色分隔与竖向强调线、暗金渐变衬底；手机端采用 15px。
